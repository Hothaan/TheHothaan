import { css } from "@emotion/react";

export const globalStyles = css`
  body,
  html {
    font-family: "Pretendard", sans-serif;
  }

  button {
    cursor: pointer;
    padding: 0;
    border: none;
  }
`;
