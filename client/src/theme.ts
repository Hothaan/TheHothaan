import { Theme } from "@emotion/react";

export const theme: Theme = {
  colors: {
    primary: "#486284",
    secondary: "#ADBDD2",
    background: "#EFF2F6",
    text: "#333333",
  },
  spacing: (factor) => `${factor * 8}px`,
};
